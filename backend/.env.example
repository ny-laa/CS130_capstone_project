# Twilio account auth token — used to verify X-Twilio-Signature on inbound webhooks.
# Copy this file to .env and fill in your value.
# Behind a proxy (ngrok, Cloudflare), run uvicorn with --proxy-headers so the
# request URL matches the public URL Twilio signed against.
TWILIO_AUTH_TOKEN=